export enum Environments {
  LOCALHOST = "https://localhost:5001",
  TEST = "https://api.test.racelab.app",
  PROD = "https://api.racelab.app",
}

export enum AuthEnvironments {
  TEST = "https://auth.racelab.app",
  PROD = "https://auth.racelab.app",
}

export enum LFMEnvironments {
  PROD = "https://lfm.racelab.app/",
}

export enum FuelSharingEnvironments {
  PROD = "https://fuel.racelab.app",
}
